version: "3.9"

services:
  bresolin_app:
    build:
      context: ./backend          # Pasta do código Flask
      dockerfile: Dockerfile      # Dockerfile dentro de backend
    container_name: bresolin_app
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env            # Usa o .env dentro de backend
    depends_on:
      - bresolin_db
    volumes:
      - ./backend:/app            # Monta a pasta backend no container

  bresolin_db:
    image: postgres:16
    container_name: bresolin_db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 95443291!
      POSTGRES_DB: bresolin_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
